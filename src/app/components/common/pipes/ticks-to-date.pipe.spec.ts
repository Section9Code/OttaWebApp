import { TicksToDatePipe } from './ticks-to-date.pipe';

describe('TicksToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TicksToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
